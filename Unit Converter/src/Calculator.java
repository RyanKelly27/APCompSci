import java.awt.*;import BreezyGUI.*;public class Calculator extends GBFrame {   	//The labels: these display the text strings that guide the user.    	private Label celsiusLabel;    	private Label fahrenheitLabel;   	private Label answerLabel;   	   	//The integer fields: these are used for the input and output of    	//integer values.  	private DoubleField celsiusField;    	private DoubleField fahrenheitField;    	private DoubleField answerField;    	//The command buttons: these trigger the buttonClicked method when   	//clicked.   	private Button fahrenheitButton; 	private Button celsiusButton; 	private Button clearButton; 	 	// A text area to output the answers.  You can put text into a TextArea  	// either by using area.setText(somestring) which replaces all of the text 	// with the new text, or area.append(somestring) which adds the new string 	// to the text already present in the text area 	      	public Calculator( )   	{   		celsiusLabel = addLabel("Celsius",1,1,1,1);   		fahrenheitLabel = addLabel("Fahrenheit",2,1,1,1);   		   		celsiusField = addDoubleField(0,1,2,1,1);   		fahrenheitField = addDoubleField(0,2,2,1,1);   		   		fahrenheitButton = addButton("Celsius to Fahrenheit",4,1,1,1);   		celsiusButton = addButton("Fahrenheit to Celsius",5,1,1,1);   		clearButton = addButton("Clear",6,1,1,1);   	}      	public void buttonClicked (Button buttonObj)   	{      		// get the values in each of the fields      		double celsiusNumber = celsiusField.getNumber();      		double fahrenheitNumber = fahrenheitField.getNumber();      		     		// now figure out which button was clicked      		if(buttonObj == clearButton)      		{      			celsiusField.setNumber(0);      			fahrenheitField.setNumber(0);      		}      		else if (buttonObj == fahrenheitButton)      		{        			fahrenheitNumber = celsiusNumber * 1.8 + 32;      			fahrenheitField.setNumber(fahrenheitNumber);      		}      		else if (buttonObj == celsiusButton)      		{      			celsiusNumber = (fahrenheitNumber - 32) * (5.0 / 9);      			celsiusField.setNumber(celsiusNumber);      		}    }        public static void main (String[] args)	{		//Instantiate the GUI part		Frame frm = new Calculator();    		//Set the application's window width and height in pixels		frm.setSize (550, 400);  		//Make the window visible to the user		frm.setVisible (true);           	}      	 }